[project]
name = "python-week1"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Robert Janaszek", email = "robert.janaszek@gmail.com" }
]
requires-python = ">=3.14"
dependencies = []

[project.scripts]
python-week1 = "python_week1:main"

[build-system]
requires = ["uv_build>=0.12.17,<0.13.0"]
build-backend = "uv_build"

[dependency-groups]
dev = [
    "ruff>=0.16.8",
]
